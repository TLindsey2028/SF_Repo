({
    getTimedDirectLink: function (cmp, event, helper) {
        helper.getTimedDirectLink(cmp, event);
    },
    wrapLinks: function (cmp, event, helper) {
        helper.wrapLinks(cmp, event);
    }
})